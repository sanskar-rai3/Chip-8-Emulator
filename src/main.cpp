#include "chip8.hpp"

#include <cstdlib>
#include <iostream>

int main(int argc, char *argv[]) {
    if (argc != 4) {
        std::cerr << "error: Usage: " << argv[0] << " <Scale> <Delay> <ROM Path>\n";
        std::exit(EXIT_FAILURE);
    }


	return 0;
}
